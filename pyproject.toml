[project]
name = "ultimate-bug-scanner"
version = "0.0.0"
description = "Utility tooling for the Ultimate Bug Scanner project"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.13,<3.14"
dependencies = []

[tool.uv]
managed = true
python-preference = "only-managed"
